[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "meaning-mesh-url-fetcher"
version = "0.1.0"
description = "Meaning-Mesh URL fetcher Lambda"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q"
